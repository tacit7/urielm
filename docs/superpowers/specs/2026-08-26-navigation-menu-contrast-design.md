# Navigation menu contrast refinement

Both navigation dropdown panels currently use `bg-base-100`, matching the application canvas and reducing perceived separation. Change the mobile More menu and account menu surfaces to `bg-base-200` in both Tokyo themes. Retain their existing shadow as the sole elevation treatment, along with all spacing, interaction, typography, active states, and responsive behavior.
