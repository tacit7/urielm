# Navigation semantics implementation plan

1. Extend navigation contract tests with skip-link, horizontal-menu, and dropdown-wrapper expectations.
2. Update the Svelte navbar and global layout with the minimum semantic markup changes.
3. Run focused JS and LiveView tests, build assets, run the UI detector once, and execute `mix precommit`.
4. Commit only task-owned files and complete EITS task 9258.
