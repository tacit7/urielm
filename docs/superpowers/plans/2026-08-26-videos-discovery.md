# Videos Discovery Implementation Plan

1. Add failing LiveView tests for `/videos`, collection grouping, filters, access labels, and empty states.
2. Add the `:videos` ShellLive route, child mapping, page state, and metadata.
3. Implement `UrielmWeb.VideosLive` with URL-driven discovery, streams, responsive daisyUI cards, provider thumbnails, and accessible empty states.
4. Split Videos and Courses in the Svelte navbar and path-to-page mapping.
5. Build assets, run focused tests and `mix precommit`, then commit and log the result in EITS.
